# Gemini API Configuration
GEMINI_API_KEY=your-api-key-here

# Logging Configuration (all optional)
# GEMINI_LOG_ENABLED=true          # Enable logging (default: false)
# GEMINI_LOG_TYPE=both             # Log type: fifo, file, both, console (default: fifo)
# GEMINI_LOG_LEVEL=debug           # Log level: debug, info, warn, error (default: info)
# GEMINI_LOG_FILE=logs/gemini.log  # Log file path (default: logs/gemini-repl.log)
# GEMINI_LOG_FIFO=/tmp/repl.fifo   # FIFO path (default: /tmp/gemini-repl.fifo)