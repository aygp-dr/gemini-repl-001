{:paths ["src" "resources"]
 :deps {org.clojure/clojure    {:mvn/version "1.12.6"}
        org.clojure/spec.alpha {:mvn/version "0.5.238"}}

 :aliases
 {;; shadow-cljs.edn uses {:deps {:aliases [:cljs :dev]}}
  :cljs {:extra-deps {thheller/shadow-cljs    {:mvn/version "3.5.1"}
                      org.clojure/test.check {:mvn/version "1.1.3"}}}

  ;; REPL/dev classpath (dev/user.cljs instruments all s/fdef'd fns)
  :dev  {:extra-paths ["dev" "test"]
         :extra-deps {org.clojure/test.check {:mvn/version "1.1.3"}}}

  ;; bb test -> npx shadow-cljs compile test && node target/test.js
  ;; (tests are cljs, so shadow-cljs's :node-test build is the runner)
  :test {:extra-paths ["test"]
         :extra-deps {org.clojure/test.check {:mvn/version "1.1.3"}}}

  ;; bb lint  ->  clojure -M:lint --lint src test dev
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2026.08.04"}}
         :main-opts ["-m" "clj-kondo.main"]}

  ;; bb fmt / bb fmt:fix  ->  clojure -M:fmt check|fix src test dev
  :fmt  {:replace-deps {dev.weavejester/cljfmt {:mvn/version "0.16.5"}}
         :main-opts ["-m" "cljfmt.main"]}}}
